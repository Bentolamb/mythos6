[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mythos6"
version = "0.1.0"
description = "Fable/Mythos 6 -- efficiency-first small language model"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
